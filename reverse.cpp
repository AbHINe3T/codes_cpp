#include <string>
#include <vector>

using namespace std;

int main() {
//
// your code will be inserted here
	ios::sync_with_stdio(false);
	int a, b;
	cin >> a >> b;
	cout << a + b << "/n";
	return 0;
}